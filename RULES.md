# 세종요리제과학원 시스템 핵심 개발 규칙 (AI 가이드라인)

이 문서는 시스템을 유지보수하는 모든 개발자 및 AI 어시스턴트가 반드시 숙지하고 따라야 하는 절대적인 최우선 규칙입니다.

## 1. 결제일(붉은선 네모박스) 발생 규칙의 절대 기준
- **기준점 (Source of Truth):** `sheet.html` (월간 출석부)
- 수강료 납부대장(`tuition.html`, `tuition_v4.js`)과 문자 발송 달력(`sms.html`, `sms_v3.js`) 등 결재일을 계산하는 모든 서브 시스템은 **무조건 `sheet.html`의 내부 수학 공식과 100% 동일하게 일치**해야 합니다.
- 양방향 동기화가 즉석에서 진행되어야 하며, 어느 화면에서 수기 출석을 입력하든 오차가 발생해서는 안 됩니다.

## 2. 제과제빵(통합) 결재일 수학 공식
- 제과제빵 등 듀얼 코스(isDualCourse, isDualBakery)의 결재 주기 공식은 다음과 같습니다.
  - **1회차 결재일:** 누적 출석 `17`회 도달 시 발생
  - **2회차 결재일:** 누적 출석 `33`회 (17 + 16) 도달 시 발생
  - **3회차 결재일:** 누적 출석 `49`회 (33 + 16) 도달 시 발생
- 즉, **첫 결제는 17회**, 그 이후부터는 **16회 간격**으로 주기가 돕니다. (`limit: 16`, `trigger: 17`)
- 이 수치(`170`, `160`)는 전체 JS 파일(`sheet.html`, `tuition_v4.js`, `sms_v3.js` 등)에서 절대 임의로 수정해서는 안 됩니다.

## 3. 당월 수기 출석 합산 누락 방지 (2026-06-08 치명적 버그 픽스 내역)
- `tuition_v4.js`와 `sms_v3.js`에서 자체 시뮬레이션(fallback)을 돌릴 때, **현재 진행 중인 달(당월)**의 출석 기록을 검사하는 로직(`isRegularCurrent` 등)에 수기 출석 기호가 반드시 포함되어야 합니다.
- **필수 포함 기호:** `'O'`, `'o'`, `'O^'`, `'o^'`, `'10'`, `'12'` 등
- 위 기호들이 카운트에서 누락되면 17회를 채워도 16회로 인지하여 결재일이 늦춰지는 치명적 수학 오류가 발생하므로, 향후 로직 리팩토링 시에도 **반드시 수기 기호 합산 로직(isPresentExt)**을 유지해야 합니다.

## 4. [CRITICAL] 코드 수정 후 자동 배포 스크립트 실행 규칙 (AI 필수 지침)
- AI 어시스턴트는 이 프로젝트의 어떤 파일(HTML, JS, CSS 등)이든 **코드를 수정하거나 생성한 직후에는 반드시, 예외 없이 아래의 명령어를 터미널(bash)에서 실행**해야 합니다.
- **실행할 명령어:** `bash "시스템_시작.command"`
- **목적:** Next.js 캐시 초기화, 정본 파일 동기화, 서버 재시작, GitHub 자동 백업 및 Vercel 실서버 반영.
- **주의사항:** 사용자가 굳이 "배포해줘"라고 말하지 않아도, 코드를 수정했다면 AI 스스로 위 명령어를 백그라운드 작업으로 실행하고 사용자에게 "버전 2026xxxx 배포가 완료되었습니다"라고 안내해야 합니다. 이를 누락하는 것은 금지됩니다.
