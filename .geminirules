CRITICAL: ALWAYS run `./시스템_시작.command` AT THE END of EVERY task where you modify code. Never skip this step. This script handles automatic deployments, Git backups, file synchronization, and browser updates.
