const fs = require('fs');

// We need to simulate the exact logic. Let's just create a dummy data set where course fallback matters,
// or hasAnyAttendance matters. We fixed those. What else could it be?
console.log("Testing discrepancy");
