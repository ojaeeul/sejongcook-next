const fs = require('fs');
const code = fs.readFileSync('Sejong/SejongAttendance/public/sheet.html', 'utf-8');

// I will mock the browser environment and run the code up to the point of rendering
// to see if it throws!

